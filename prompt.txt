Act as a Senior AI Architect. I want to modify my existing MERN skin detection project to follow the "Local Inference" architecture found in professional repositories like MahimaKhatri's. 

I want to stop using Hugging Face and instead host the model myself.

1. PROJECT STRUCTURE:
   - /client: React (Vite)
   - /ai-server: Python (FastAPI) to handle AI predictions.
   - /web-server: Node.js (Express) to handle Auth and MongoDB history.

2. AI BACKEND (main.py):
   - Use 'torch' to load an 'EfficientNet-B0' model (.pt file).
   - Implement a /predict endpoint that takes an image and performs ImageNet normalization (mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]).
   - Return the diagnosis, confidence score, and a brief "Medical Insight" for the class.

3. WEB BACKEND (index.js):
   - Update my Express routes to act as a bridge. When a user uploads an image, the Node server sends it to the FastAPI server, gets the result, and saves it to MongoDB before returning it to the user.

4. FRONTEND (Scanner.jsx):
   - Create a professional "Diagnostic UI" with a progress ring.
   - Use Axios to hit the Node server.
   - Map the 7 categories (akiec, bcc, bkl, df, mel, nv, vasc) to friendly English names.

5. CONFIG:
   - Provide a Dockerfile for the ai-server.
   - Provide a netlify.toml for the client.

Please generate the full code for main.py (FastAPI), the updated Express controller, and the React Scanner component.